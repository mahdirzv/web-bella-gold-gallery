# gold_gallery_uk Instagram assets

Collected from the public `gold_gallery_uk` Instagram profile with Bright Data on
3 September 2026.

- 35 reels discovered; all 35 cover images downloaded.
- 33 static post images downloaded from the authenticated profile grid.
- 1 profile image and 2 highlight covers downloaded.
- 71 unique image files in total, representing all 68 profile posts.

Bright Data's posts discovery collector returned `Unable to discover older
posts` (`dead_page`) for the older static posts, both synchronously and
asynchronously. The separate reels discovery collector completed successfully.
The missing static posts were recovered from the authenticated Instagram grid.

See `manifest.json` for the collection summary, `static-posts.json` for the
static-image/source mapping, and `brightdata-reels-history.json` for the reel
records.
